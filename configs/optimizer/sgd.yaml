algorithm : fit_sgd
fit_kwargs:
  optimizer   :
    _target_: optax.sgd
    learning_rate : 1e-2
    momentum      : 0.15
  batch_size  : 200
  num_epochs  : 100
  key         :
    _target_  : jax.random.PRNGKey
    seed      : 0